*** Begin Patch
*** Update File: frontend/src/App.js
@@ function App() {
   const [navigationHistory, setNavigationHistory] = useState([]);
+  // State for search term in the corporate hierarchy tree. This allows users to quickly
+  // filter family tree members by typing part of a company name or D-U-N-S number.
+  // A search box makes tree navigation more efficient, even for relatively small trees【223048328786256†L68-L70】.
+  const [treeSearchTerm, setTreeSearchTerm] = useState("");
@@
-                                 {/* Family Tree */}
-                                 {hierarchy.familyTreeMembers && hierarchy.familyTreeMembers.length > 0 && (
-                                   <div>
-                                     <h5 className="font-medium text-gray-900 mb-2">{"Family Tree"} ({hierarchy.familyTreeMembers.length} {"members"})</h5>
-                                     <div className="bg-gray-50 border border-gray-200 rounded-md p-3 max-h-48 overflow-y-auto">
-                                       {hierarchy.familyTreeMembers.map((member, index) => (
-                                         <div key={index} className="flex justify-between items-center py-2 border-b last:border-b-0 hover:bg-gray-100 cursor-pointer transition-colors rounded px-2" onClick={() => navigateToCompany(member.duns, member.primaryName)}>
-                                           <div className="flex-1">
-                                             <p className="font-medium text-gray-800 hover:text-blue-600">{member.primaryName}</p>
-                                             <p className="text-sm text-gray-600">DUNS: {member.duns}</p>
-                                             {member.hierarchyLevel !== undefined && (
-                                               <span className="inline-block px-2 py-1 bg-gray-200 text-gray-700 text-xs rounded mt-1">
-                                                 {"Level"}: {member.hierarchyLevel}
-                                               </span>
-                                             )}
-                                           </div>
-                                           <div className="ml-2">
-                                             <svg className="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
-                                               <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />
-                                             </svg>
-                                           </div>
-                                         </div>
-                                       ))}
-                                     </div>
-                                   </div>
-                                 )}
+                                 {/* Family Tree with search and filtering */}
+                                 {hierarchy.familyTreeMembers && hierarchy.familyTreeMembers.length > 0 && (
+                                   <div>
+                                     <h5 className="font-medium text-gray-900 mb-2">
+                                       {"Family Tree"} ( {hierarchy.familyTreeMembers.length} {"members"} )
+                                     </h5>
+                                     {/* Search bar for filtering the hierarchy tree */}
+                                     <div className="mb-2">
+                                       <input
+                                         type="text"
+                                         value={treeSearchTerm}
+                                         onChange={(e) => setTreeSearchTerm(e.target.value)}
+                                         placeholder="Search in hierarchy..."
+                                         className="w-full px-3 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-blue-500 focus:border-transparent text-sm"
+                                       />
+                                     </div>
+                                     {(() => {
+                                       const term = treeSearchTerm.trim().toLowerCase();
+                                       const filtered = hierarchy.familyTreeMembers.filter((member) => {
+                                         if (!term) return true;
+                                         const name = member.primaryName?.toLowerCase() || "";
+                                         const duns = member.duns?.toLowerCase() || "";
+                                         return name.includes(term) || duns.includes(term);
+                                       });
+                                       return (
+                                         <div className="bg-gray-50 border border-gray-200 rounded-md p-3 max-h-48 overflow-y-auto">
+                                           {filtered.length > 0 ? (
+                                             filtered.map((member, index) => (
+                                               <div
+                                                 key={index}
+                                                 className="flex justify-between items-center py-2 border-b last:border-b-0 hover:bg-gray-100 cursor-pointer transition-colors rounded px-2"
+                                                 onClick={() => navigateToCompany(member.duns, member.primaryName)}
+                                               >
+                                                 <div className="flex-1">
+                                                   <p className="font-medium text-gray-800 hover:text-blue-600">{member.primaryName}</p>
+                                                   <p className="text-sm text-gray-600">DUNS: {member.duns}</p>
+                                                   {member.hierarchyLevel !== undefined && (
+                                                     <span className="inline-block px-2 py-1 bg-gray-200 text-gray-700 text-xs rounded mt-1">
+                                                       {"Level"}: {member.hierarchyLevel}
+                                                     </span>
+                                                   )}
+                                                 </div>
+                                                 <div className="ml-2">
+                                                   <svg className="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
+                                                     <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />
+                                                   </svg>
+                                                 </div>
+                                               </div>
+                                             ))
+                                           ) : (
+                                             <div className="text-center py-4 text-sm text-gray-500">
+                                               {"No matching members"}
+                                             </div>
+                                           )}
+                                         </div>
+                                       );
+                                     })()}
+                                   </div>
+                                 )}
*** End Patch
